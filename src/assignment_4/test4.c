
// expression
int main() {
    float a = 2.3, b = 3.4, result = 0.0;

    // 수식 계산
    result = ((++a + b) * (a + b--)) / (2 * a - 3 * b;

    // 결과 출력
    printf("결과: %.2lf\n", result);

    return 0;
}

